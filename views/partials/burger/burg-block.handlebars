<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-newAte="{{ate}}">
		{{#if ate}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>
	<button class="delete" data-id="{{id}}">
		DELETE
	</button>
</li>
